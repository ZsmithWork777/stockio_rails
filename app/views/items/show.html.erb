<h1><%= @item.name %></h1>

<p>
  We currently have <%= @item.quantity %> in stock.
</p>

<%= link_to "Edit Item", edit_item_path(@item) %> |
<%= link_to "Delete Item", item_path(@item),
      data: { turbo_method: :delete,
              turbo_confirm: "Are you sure you want to delete this item?" } %> |
<%= link_to "Back to Inventory", items_path %>